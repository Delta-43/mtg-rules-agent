# GitHub auto-requests these owners as reviewers on any PR touching a
# matching path; the LAST matching pattern wins, so more specific paths
# belong below more general ones.

# Default: repo owner reviews anything not claimed by a more specific rule below.
*                       @Delta-43
