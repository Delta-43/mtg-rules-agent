# Note: Chroma/PDF-parsing deps (chromadb, langchain-chroma, langchain-text-splitters,
# pdfminer.six, beautifulsoup4) moved to rules_mcp/requirements.txt -- the main
# backend no longer touches ChromaDB directly, it calls rules-mcp over MCP instead.
langchain>=1.0.0,<2.0.0
langchain-community>=0.4.0
langchain-ollama>=1.0.0
langchain-openai>=0.2.0
langchain-mcp-adapters>=0.1.0
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
trafilatura>=1.12.0
requests>=2.31.0
PyYAML>=6.0.1
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
slowapi>=0.1.9
# Observability (see docs/OBSERVABILITY_PLAN_V2.md) -- HTTP metrics exposed at
# GET /metrics, scraped by the opt-in `prometheus` compose service.
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0
# Only used by tests/ (FastAPI TestClient needs httpx; pytest runs the suite).
pytest>=8.0.0
httpx>=0.27.0
# Scaffolding for the future accounts/tiers/billing pivot (accounts_db/) --
# not imported by app_api yet, see PLAN.md. sqlalchemy/alembic/asyncpg build
# and query the new Postgres tables; psycopg2-binary is Alembic's sync
# migration-runner driver; pyjwt will verify GoTrue-issued JWTs in Phase 1.
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pyjwt>=2.8.0
